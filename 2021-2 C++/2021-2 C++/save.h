#ifndef _SAVE
#define _SAVE

#include <iostream>
#include <vector>
#include <string_view>
#include <filesystem>
#include <fstream>
#include <ctime>

void Save(std::vector<std::string> saveList, std::string saveDate);

#endif